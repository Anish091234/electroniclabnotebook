# Copy to .env.<firebase-project-id> for deployment or .env.local for emulators.
# Must match VITE_FIREBASE_FUNCTIONS_REGION in the corresponding web app.
FUNCTIONS_REGION=us-central1
PUBLIC_APP_ORIGIN=http://localhost:5173
ENFORCE_APP_CHECK=false
